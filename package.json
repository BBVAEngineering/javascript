{
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "scripts": {
    "commit": "git-cz",
    "lint": "cd eslint-config-bbva && npm run lint && cd .. && cd eslint-plugin-bbva && npm run lint && cd ..",
    "postinstall": "cd eslint-config-bbva && npm install && cd .. && cd eslint-plugin-bbva && npm install && cd ..",
    "semantic-release": "semantic-release",
    "test": "cd eslint-config-bbva && npm run test && cd .. && cd eslint-plugin-bbva && npm run test && cd .."
  },
  "devDependencies": {
    "@commitlint/cli": "^7.5.2",
    "@commitlint/config-conventional": "^7.5.0",
    "commitizen": "^3.0.7",
    "cz-conventional-changelog": "^2.1.0",
    "eslint": "^3.9.1",
    "eslint-config-bbva": "^2.0.0",
    "eslint-plugin-node": "^8.0.1",
    "husky": "^1.3.1",
    "istanbul": "^0.4.5",
    "lerna": "^3.13.1",
    "mocha": "^3.1.2",
    "nyc": "^13.3.0",
    "remark-cli": "^6.0.1"
  },
  "name": "javascript",
  "dependencies": {
    "eslint-config-bbva": "file:packages/eslint-config-bbva",
    "eslint-plugin-bbva": "file:packages/eslint-plugin-bbva"
  }
}
